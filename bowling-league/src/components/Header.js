// Functional component for the header section of the page
const Header = () => {
    return (
        // The <header> element typically represents the introductory content or navigation of a page
        <header>
            {/* Main title of the page */}
            <h1>Bowling League - Marlins & Sharks</h1>
        </header>
    );
};

// Export the Header component so it can be used in other parts of the application
export default Header;
